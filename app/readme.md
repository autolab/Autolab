

fix:
for all new user creation, prompt for a user for password, map mimic how afs(cmu computing service which already has a user account for everyone) work in beta (aws instance, no user accoutn every one logs in as ubuntu)