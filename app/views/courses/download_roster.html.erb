<% for user in @users do %>
<%= "#{@course.semester},#{user.andrewID},#{user.last_name},#{user.first_name},#{user.school},#{user.major},#{user.year},#{user.grade_policy},#{user.lecture},#{user.section}" %><br>
<% end %>
