<% @title = "Build Autograder Image" %>

<%= render partial: "new_image",
           locals: {
             form_model: [@course, @container_image],
             admin: false,
             templates: ContainerImage.where(is_public: true)
           } %>
